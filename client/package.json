{
  "name": "client",
  "version": "1.0.0",
  "description": "Rate limiting client example",
  "main": "dist/src/singleClient.js",
  "author": "Blagoj Dimovski",
  "license": "MIT",
  "dependencies": {
    "semaphore-lib": "git+https://github.com/akinovak/semaphore-lib.git#dev",
    "socket.io-client": "^4.2.0",
    "axios": "^0.21.4"
  },
  "devDependencies": {
    "@types/node": "^16.9.4",
    "tslint": "^6.1.3",
    "typescript": "^4.4.3"
  },
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "singleClient": "node dist/src/singleClient.js",
    "multiClient": "node dist/src/multiClient.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write \"**/*.{js,json,md,sol,ts}\""
  }
}
